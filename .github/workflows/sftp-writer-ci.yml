name: SFTP Writer CI

on:
  push:
    paths:
      - 'services/sftp-writer/**'
  pull_request:
    paths:
      - 'services/sftp-writer/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Build
        run: go build ./services/sftp-writer/...
