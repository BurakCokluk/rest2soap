name: REST API CI

on:
  push:
    paths:
      - 'services/rest-api/**'
  pull_request:
    paths:
      - 'services/rest-api/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Build
        run: go build ./services/rest-api/...
